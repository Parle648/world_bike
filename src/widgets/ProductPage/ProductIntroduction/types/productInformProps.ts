import { Dispatch, SetStateAction } from "react";

interface productInformProps {
    articul: string,
    background_img: string,
    cost: string,
    description: string,
    id: number,
    in_stock: boolean,
    title: string,
};

export default productInformProps;