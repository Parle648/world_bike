import { ReactNode } from "react";

type dropDownProps = {
    order_date: string,
    order_id: string,
    order_amount: string,
    order_status: string,
    trigger_bacground: string,
    children: ReactNode
};

export default dropDownProps;